//struct Point{
   // double x;
    // double y;
 
    //std::string to_s() const {
    //                 ^
        //return "(" + std::to_string(x) + ", " + std::to_string(y) + ")";
    //}
 
    //double distance(Point p) const {
    //                       ^
       // return std::sqrt(std::pow((x - p.x), 2) + std::pow((y - p.y), 2));
    //}
//}; 

//When member functions do not alter the value of the variable on which they are called (i.e. they do not alter the value of its member data), they should be marked as const