#include <iostream>
#include <string>

int main(){
    std::string s1, s2;
    s1 = "3";
    s2 = "4";
    std::cout << s1 + s2 << std::endl;
}

// the + represents combining the two strings together so s1s2 consecutively without gap, hence printing 34